<%= form_with(model: listing, url: url, method: method, class: "space-y-6") do |form| %>
  <% if listing.errors.any? %>
    <div class="bg-red-50 border border-red-200 rounded-md p-4">
      <h2 class="text-red-800 font-semibold mb-2"><%= pluralize(listing.errors.count, "error") %> prevented this listing from being saved:</h2>
      <ul class="list-disc list-inside text-red-700 text-sm">
        <% listing.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, class: "form-label" %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div>
    <%= form.label :discipline, class: "form-label" %>
    <%= form.select :discipline, Listing.disciplines.keys.map { |d| [d.titleize, d] }, { include_blank: "Select a discipline" }, class: "form-control" %>
  </div>

  <div>
    <%= form.label :description, class: "form-label" %>
    <%= form.rich_text_area :description, class: "w-full" %>
  </div>

  <div>
    <%= form.label :commitment, class: "form-label" %>
    <%= form.text_field :commitment, class: "form-control", placeholder: "e.g. ~5 hrs/week, One-time project, Flexible" %>
  </div>

  <div>
    <%= form.label :location, class: "form-label" %>
    <%= form.text_field :location, class: "form-control", placeholder: "Remote" %>
  </div>

  <div>
    <%= form.label :skills, class: "form-label" %>
    <%= form.text_field :skills, class: "form-control", placeholder: "e.g. Ruby, Rails, React" %>
  </div>

  <div>
    <%= form.label :status, class: "form-label" %>
    <%= form.select :status, Listing.statuses.keys.map { |s| [s.titleize, s] }, {}, class: "form-control" %>
  </div>

  <div class="flex items-center gap-4">
    <%= form.submit class: "bg-indigo-600 text-white px-4 py-2 rounded-md hover:bg-indigo-700 transition cursor-pointer" %>
    <% if listing.persisted? %>
      <%= link_to "Cancel", listing_path(listing), class: "text-gray-600 hover:text-gray-800" %>
    <% end %>
  </div>
<% end %>
